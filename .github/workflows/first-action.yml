name: First-workflow
on: workflow_dispatch
#allow you to manually trigeer a workflow
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "hello world "
      - name: print bye
        run: "bye"
